# Copy this to .env and fill in real values. Never commit .env itself.

# Supabase (apps/mobile + packages/ai-router cache backend)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI providers (packages/ai-router)
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Cloudflare (only needed once the API gateway layer is added)
CLOUDFLARE_API_TOKEN=
